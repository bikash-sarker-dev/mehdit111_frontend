import UserSettingsPage from "@/components/dashboardComponents/userSettings/UserSettings";

const page = () => {
  return (
    <div>
      <UserSettingsPage />
    </div>
  );
};

export default page;
