import GrowMoreReviewsOnboarding from "@/components/onBoardingCom/GrowMoreReviewsOnboarding";

const page = () => {
  return (
    <div>
      <GrowMoreReviewsOnboarding />
    </div>
  );
};

export default page;
